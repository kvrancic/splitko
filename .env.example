# Pexels API (provided in api-keys.txt). Used server-side only.
PEXELS_API_KEY=

# Supabase. Create a project at supabase.com, enable Email/Password auth, then paste:
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Optional. Only needed if you also enable Google OAuth in Supabase.
# (Configured in the Supabase dashboard, no env vars required here.)
